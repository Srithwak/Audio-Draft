{
  "name": "t",
  "version": "1.0.0",
  "description": "Get Keys from supabase, API_URL and ANON_PUBLIC_KEY\r Initialize all the tables in supabase\r Add indexes for performance",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "node server.js",
    "start:desktop": "electron .",
    "build": "electron-packager . Audio-Draft2 --platform=win32 --arch=x64 --out=release --overwrite --ignore=\"(^/dist$|^/dist_app$|^/release$|\\.git)\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "axios": "^1.13.6",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "spotify-web-api-node": "^5.0.2"
  },
  "devDependencies": {
    "electron": "^40.8.0",
    "electron-packager": "^17.1.2"
  }
}
